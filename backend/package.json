{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint src/",
    "docker:up": "docker-compose -p your-project-name up --build -d",
    "docker:down": "docker-compose -p your-project-name down -v"
  },
  "dependencies": {
    "@my-monorepo/types": "workspace:*",
    "@my-monorepo/utils": "workspace:*",
    "dotenv": "^17.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "tsx": "^4.7.0"
  }
}
